export function GathoLogo({ className }: { className?: string }) {
  return (
    <div className={`gathoLogo ${className}`}>
      <img src="/img/gatho-logo.svg" alt="Gatho" width="100%" />{/* eslint-disable-line */}
    </div>
  );
}
